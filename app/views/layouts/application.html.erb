<!DOCTYPE html>
<html>
<head>
	
  <title>Biker</title>
  <link rel="icon" type="image/png" href="/assets/icon.png">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <h1 class='text-center bikers-trip'><em><marquee>Biker's Trip</marquee></em></h1>
</head>

			<body class="<%= controller_name %>">
				<div class='container'>

					<% if user_signed_in? %>
						<div class='row'>
							<div class='pojok-kanan'>
								<p class='welcome'>Welcome, <%= current_user.name %> </p>
							<div class='transparent-welcome'></div>
						</div>
					</div>
				<% end %>
						
			<%= yield %>
		</div>
	</body>
</html>
